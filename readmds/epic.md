# Epic games launcher



this fetches the latest version, downloads the correct build for your system, installs it into `~/Applications`, and creates a launcher for easy access.

---
## BEFORE INSTALL 
Make sure you have xcode components installed in self service! AND you are off school wifi

---

## ⚡ Quick Install

Run this command in Terminal:

```bash
curl -fsSL https://apps.suspendednetwork.tech/installers/epic.command | bash
